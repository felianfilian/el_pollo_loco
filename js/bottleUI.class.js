class BottleUI extends Drawable {
  constructor(x, y, height, width) {
    super().loadImage("img/6_salsa_bottle/salsa_bottle.png");
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
  }
}
